Abundio MartinezHomework 3February 26, 202. Describing what the addEventListener() method is doing:const multiplyNums = () => {    let num3 = document.getElementById("num3").value;    let num4 = document.getElementById("num4").value;    let multiply = num3 * num4;    document.getElementById("result2").innerHTML = multiply;};const btnMultiply = document.getElementById("multiply");btnMultiply.addEventListener("click", multiplyNums);In this method instead of putting the onclick function in html. I am using multiplyEvenListener. I declare const variable and I add name to it. This method can accept up to 3 parameters. 3. Referenced function vs invoked/called functionReference function is something that you can use not in the moment. Most of the time is use with addEventListenerexample:const multiplyNums = () => {    let num3 = document.getElementById("num3").value;    let num4 = document.getElementById("num4").value;    let multiply = num3 * num4;    document.getElementById("result2").innerHTML = multiply;};const btnMultiply = document.getElementById("multiply");btnMultiply.addEventListener("click", multiplyNums);             A call function always has () in the end “callMeNow()”.example:const alertabundio= () => {alert(“function:HI!");}alertabundio();4. Named functions vs function expressionvar foo = function bar() { ... }var foo = function() { ... }The main difference between function declaration and function expression is the function name. A function expression can omit the name of the function and make it into an anonymous function.5. Arrow functions, named functions, and anonymous functionsa. Arrow functions:const divideNumsFixed = () => {    const num7 = document.getElementById("num7").value;    const num8 = document.getElementById("num8").value;    const divide = num7 / num8;    const divideFixed = divide.toFixed(2);    document.getElementById("result4").innerHTML = divideFixed;};b. Anonymous functions:Anonymous functions do not have a name. They are assigned to a variable; also known asa function expression. They are not bound to “this”, but rather bound to the parent scope.This type of function has a block scope.const divideNumsFixed = () => {    const num7 = document.getElementById("num7").value;    const num8 = document.getElementById("num8").value;    const divide = num7 / num8;    const divideFixed = divide.toFixed(2);    document.getElementById("result4").innerHTML = divideFixed;};c. Named functions:Just like “var” a named function is hoisted into the browser.Code example:function namedExample() {alert("Named function");}namedExample();6. https://github.com/AbundioMartinez/DynamicWeb_hw3/tree/master/Homework37. window.location.reload()The “reload()” method reloads the document, just like clicking the reload button on yourbrowser.8. Local git command1. Use “pwd” to check that you are inside the right folder.2. “git init” initializes a local git repository on your computer.3. Once you added files inside, type “git status” on the Terminal and hit return keyboard.4.  When you want to “commit” changes type “git add .” and hit return; if you want tocommit all your files and folders5. You can “commit” individual files by typing “git add index.html”6. “git commit” can be done in 2 ways, either ‘git commit –m “comment”’ or “git commit”7. “git remote add” you can push into the remote origin, which is the remote hosting servicethat host your repository remotely.8.  “git remote add origin remoteurl” this allows you to push local commits for the first timeonto remote. Make sure to have a remote repository to push on.9. To create a remote repository, go to github and click your profile, click Repositories, andclick the button new and this will take you to create a new repository.10. Create a repository by filling repository name. Click the “create repository” button.11. “git push –u” using this you can push your local repository into remote origin. In theTerminal type “git push –u" and click return.12. You will get a master branch, which is a default branch when you initialize.m. “cd” can change the directory. Type “cd name_of_folder” in the Terminal to do so.9. Local Git repositoryGit is the local respository while subdirectory works inside the directory.10. What else signals initialized Git?The master. 